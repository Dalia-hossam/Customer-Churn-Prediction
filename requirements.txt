# Core
pandas>=2.0
numpy>=1.24
joblib>=1.3
scikit-learn>=1.9,<2     # model was trained with scikit-learn 1.9.0 — keep this in sync

# Needed to unpickle the saved pipeline (it includes an ADASYN step,
# even though ADASYN is only applied during .fit, not .predict)
imbalanced-learn>=0.12

# Streamlit app
streamlit>=1.35

google-generativeai>=0.8.5
python-dotenv>=1.0.1